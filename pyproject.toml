[project]
name = "ends-of-reading"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cycler>=0.12.1",
    "matplotlib>=3.10.8",
    "numpy>=2.5.2",
    "pandas>=2.3.3",
]

[dependency-groups]
dev = [
    "pytest>=8",
]
